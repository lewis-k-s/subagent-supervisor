interface:
  display_name: "Subagent Supervisor"
  short_description: "Dispatch and monitor supervised subagents"
  default_prompt: "Use $subagent-supervisor to split this task into supervised subprocess subtasks and report results."
